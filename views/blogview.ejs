<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
</head>
<body>
    <style>
        .box{
            max-width: 1140px;
            margin: 0 auto;
        }
        .card{
            margin: 10px;
        }
        /* .position{
            display: none;
        } */
    </style>
    <div class="box" id="box">
    <!-- <div class="card">
        <h5 class="card-header">Featured</h5>
        <div class="card-body">
          <p class="card-text">With supporting text below as a natural lead-in to additional content.</p>
          <footer class="blockquote-footer">Someone famous in <cite title="Source Title">Source Title</cite></footer>
          
          <a href="#" class="btn btn-primary">New Add</a>
          <a href="#" class="btn btn-primary">delete</a>
        </div>
      </div> -->
    </div>
    <script>
        
        
        let data = async ()=> {
            try {
                let req = await fetch(`http://localhost:7777/blog/view`)
                let res = await req.json();
                let blog = res.cart
                
                let temp =``;
                for (let i = 0; i < blog.length; i++) {
                    let title = blog[i].title;
                    let discripction =blog[i].discripction
                    let authorname =blog[i].authorname
                    temp +=`<div class="card">
                                <h5 class="card-header">${title}</h5>
                                    <div class="card-body">
                                        <p class="card-text">${discripction}</p>
                                            <footer class="blockquote-footer">${authorname}</cite></footer>
                                        <a href="/editblog" class="btn btn-primary">Edit</a>
                                        <a href="#" class="btn btn-primary">delete</a>
                                    </div>
                            </div>`;
                }
                document.getElementById('box').innerHTML=temp
                console.log(res.cart);
                
            } catch (error) {
                console.log(error);
            }
        }
        
        data()
        


        let get =async()=>{
            let req = await fetch(`http://localhost:7777/blog/view`)
                let res = await req.json();
                console.log(res);
        }
        get()
    </script>
</body>
</html>